[build]
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"
  # Function timeout in seconds (max 60 for Pro plan)
  timeout = 60

# Optional: specific function timeout
[functions."claude"]
  timeout = 60

[functions."scheduled-predictions"]
  timeout = 60
